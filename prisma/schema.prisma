generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.sqlite"
}

model appointments {
  id       Int      @id @default(autoincrement())
  name     String
  service  String
  phone    String
  date     String
  canceled Boolean
}